services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
      target: deps
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    volumes:
      - ./backend:/app
    environment:
      DEBUG: "true"
    ports:
      - "8000:8000"
