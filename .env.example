# Banco de dados SQLite (caminho relativo ao diretório de execução)
DATABASE_URL=sqlite+aiosqlite:///./estoque.db

# Modo debug (false em produção)
DEBUG=false

# Nome exibido na interface
APP_NAME=Estoque
